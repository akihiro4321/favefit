# プラン作成周りの画面フロー

## 概要

FaveFitアプリケーションにおける14日間の食事プラン作成から承認までの画面フローを説明します。

## 画面フロー図

```mermaid
flowchart TD
    Start([ユーザーがプランページにアクセス]) --> CheckPlan{プランが存在するか?}
    
    CheckPlan -->|なし| NoPlan[プランなし画面]
    NoPlan -->|「プランを作成する」ボタンクリック| GeneratePlan[プラン生成API呼び出し]
    
    GeneratePlan --> Generating[プラン生成中<br/>バックグラウンド処理]
    Generating -->|生成完了| PendingPlan[Pending状態のプラン表示]
    
    CheckPlan -->|Pending状態| PendingPlan
    CheckPlan -->|Active状態| ActivePlan[Active状態のプラン表示]
    
    PendingPlan --> PlanSummary[プラン概要カード表示]
    PlanSummary --> PlanDetails[日別カード詳細表示]
    PlanDetails --> ApprovalButtons[承認/拒否ボタン]
    
    ApprovalButtons -->|「このプランで進める」クリック| ApproveAPI[承認API呼び出し]
    ApprovalButtons -->|「別のプランを生成する」クリック| RejectAPI[拒否API呼び出し]
    
    ApproveAPI --> StatusUpdate[プランステータスを<br/>pending → activeに変更]
    StatusUpdate --> BackgroundGen[レシピ詳細生成開始<br/>バックグラウンド処理]
    BackgroundGen --> ActivePlan
    
    RejectAPI --> ConfirmDialog[確認ダイアログ]
    ConfirmDialog -->|確認| ArchivePlan[プランをarchivedに変更]
    ArchivePlan --> NoPlan
    
    ActivePlan --> DayCards[日別カード簡易表示]
    DayCards --> Actions[アクションボタン]
    Actions --> Refresh[一括再生成]
    Actions --> Boredom[飽きたボタン]
    
    Refresh -->|新規プラン生成API呼び出し| Generating
    Generating -->|生成完了| PendingPlan
    
    style PendingPlan fill:#fff3cd
    style ActivePlan fill:#d1e7dd
    style Generating fill:#cfe2ff
    style BackgroundGen fill:#cfe2ff
    style Refresh fill:#f8d7da
```

## 画面状態の詳細

### 1. プランなし状態 (`/plan`)

**表示内容:**
- カレンダーアイコン
- 「プランがありません」メッセージ
- 「まずは2週間のプランを作成しましょう」説明文
- 「プランを作成する」ボタン

**ユーザーアクション:**
- 「プランを作成する」ボタンをクリック

**API呼び出し:**
```
POST /api/plan/generate
{
  "userId": "user-id"
}
```

**処理フロー:**
1. バックグラウンドでプラン生成を開始
2. プラン基本情報（14日分のメニュー、栄養価、タグ）を生成
3. `status: "pending"`でプランを保存
4. 買い物リストは生成しない（承認後に生成）

---

### 2. Pending状態のプラン表示

**表示内容:**

#### ヘッダー
- 「2週間プラン」タイトル
- 開始日
- 「承認待ち」バッジ

#### プラン概要カード (`PlanSummary`コンポーネント)
- **14日間の平均栄養価**
  - 平均カロリー（目標との差分%表示）
  - 平均タンパク質(g)
  - 平均脂質(g)
  - 平均炭水化物(g)
- **使用ジャンル・タグ**
  - タグの出現頻度（上位10個）
  - 例: 「和食 (8回)」「洋食 (6回)」
- **チートデイ情報**
  - チートデイの日付一覧

#### 日別カード（詳細表示）
各日のカードに以下を表示:
- 日付と曜日
- 1日の合計カロリーとPFC
- 各食事（朝・昼・夜）の詳細:
  - **レシピタイトル（全文表示）**
  - **タグ（最大3つ、バッジ表示）**
  - **栄養価（カロリー、PFC）**

#### 承認/拒否ボタン
- 「このプランで進める」ボタン（プライマリ）
- 「別のプランを生成する」ボタン（アウトライン）

**ユーザーアクション:**
- プラン概要と日別カードを確認
- 承認または拒否を選択

---

### 3. プラン承認フロー

#### 承認時 (`handleApprovePlan`)

**API呼び出し:**
```
POST /api/plan/approve
{
  "userId": "user-id",
  "planId": "plan-id"
}
```

**処理フロー:**
1. プランのステータスを`pending` → `active`に変更
2. バックグラウンドでレシピ詳細生成を開始:
   - 42食（14日 × 3食）のレシピ詳細を生成
   - バッチ処理: 5食ずつ、並列3件、バッチ間に1秒待機
   - 各レシピの`ingredients`と`steps`を生成
3. レシピ詳細生成完了後、買い物リストを生成:
   - すべてのレシピの材料を集計
   - カテゴリ別に分類・ソート
4. 成功メッセージを表示:
   - 「プランを承認しました。レシピ詳細を生成中です。完了まで1〜2分かかる場合があります。」

**画面遷移:**
- Active状態のプラン表示に切り替わる

#### 拒否時 (`handleRejectPlan`)

**確認ダイアログ:**
```
このプランを拒否しますか？
別のプランを生成できます。
```

**API呼び出し:**
```
POST /api/plan/reject
{
  "userId": "user-id",
  "planId": "plan-id"
}
```

**処理フロー:**
1. プランのステータスを`pending` → `archived`に変更
2. プランなし状態に戻る

**画面遷移:**
- プランなし画面に戻る
- 再度「プランを作成する」ボタンが表示される

---

### 4. Active状態のプラン表示

**表示内容:**

#### ヘッダー
- 「2週間プラン」タイトル
- 開始日
- アクションボタン:
  - 「飽きた」ボタン（飽き防止ダイアログを開く）
  - 「一括再生成」ボタン（14日間を一括で再生成）

#### 日別カード（簡易表示）
各日のカードに以下を表示:
- 日付と曜日
- 1日の合計カロリーのみ
- 各食事（朝・昼・夜）のリンク:
  - レシピタイトル（最大100pxで切り詰め）
  - クリックでレシピ詳細ページへ遷移

**特徴:**
- レシピ詳細（`ingredients`, `steps`）が生成済み
- 買い物リストが利用可能
- レシピ詳細ページで材料と手順を確認可能

---

### 5. 一括再生成フロー

**実装:**

#### 動作
- Active状態のプランに対してのみ動作
- `POST /api/plan/generate`を呼び出し（新規プラン生成と同じAPI）
- 既存のActiveプランを自動的にArchivedに変更
- **新しいPendingプランを作成**
- **承認フローに入る**

#### 処理フロー
1. 確認ダイアログを表示:
   ```
   14日間のプランを一括で再生成しますか？
   現在のプランはアーカイブされ、新しいプランが生成されます。
   生成後、プランを確認して承認してください。
   ```
2. `POST /api/plan/generate`を呼び出し
3. バックグラウンドで新しいプランを生成:
   - 既存のActiveプランをArchivedに変更
   - 新しいPendingプランを作成（基本情報のみ）
4. Pendingプランを表示:
   - プラン概要カード
   - 日別カード詳細表示
   - 承認/拒否ボタン
5. ユーザーがプランを確認して承認
6. 承認後にレシピ詳細を生成

**特徴:**
- ✅ **一括再生成も承認フローを経由します**
- ✅ ユーザーが新しいプランを確認・承認できます
- ✅ 承認後にレシピ詳細が生成されます
- ✅ 新規プラン作成と同じフローで統一されています

---

## データフロー

### プラン生成時

```mermaid
sequenceDiagram
    participant U as ユーザー
    participant UI as プランページ
    participant API as /api/plan/generate
    participant Service as plan-service.ts
    participant AI as AI Agent
    participant DB as Firestore

    U->>UI: 「プランを作成する」クリック
    UI->>API: POST /api/plan/generate
    API->>Service: generatePlan()
    Service->>AI: 14日間のプラン生成リクエスト
    AI-->>Service: プラン基本情報（JSON）
    Service->>DB: プラン保存（status: "pending"）
    DB-->>Service: プランID
    Service-->>API: { status: "started" }
    API-->>UI: 成功レスポンス
    UI->>UI: pendingプランを取得して表示
```

### プラン承認時

```mermaid
sequenceDiagram
    participant U as ユーザー
    participant UI as プランページ
    participant API as /api/plan/approve
    participant Service as plan-service.ts
    participant AI as AI Agent
    participant DB as Firestore

    U->>UI: 「このプランで進める」クリック
    UI->>API: POST /api/plan/approve
    API->>Service: approvePlan()
    Service->>DB: ステータス更新（pending → active）
    Service->>Service: approvePlanAndGenerateDetails()<br/>（バックグラウンド）
    Service->>AI: レシピ詳細生成（バッチ処理）
    AI-->>Service: レシピ詳細（ingredients, steps）
    Service->>DB: レシピ詳細を更新
    Service->>Service: 買い物リスト生成
    Service->>DB: 買い物リスト保存
    Service-->>API: { success: true }
    API-->>UI: 成功レスポンス
    UI->>UI: activeプランを取得して表示
```

---

## 状態遷移図

```mermaid
stateDiagram-v2
    [*] --> プランなし
    
    プランなし --> プラン生成中: 「プランを作成する」クリック
    プラン生成中 --> Pending: プラン生成完了
    
    Pending --> Active: 承認
    Pending --> Archived: 拒否
    Pending --> プランなし: 拒否後（画面表示）
    
    Active --> Archived: 一括再生成（既存プラン）
    Active --> Pending: 一括再生成（新規プラン）
    Active --> Active: 飽きたボタン（部分更新）
    Active --> Archived: プラン完了時
    
    Archived --> [*]
```

---

## 主要なコンポーネント

### `PlanPage` (`src/app/plan/page.tsx`)
- プラン表示のメインコンポーネント
- 状態管理: `activePlan`, `pendingPlan`
- 条件分岐: `isPending`で表示を切り替え

### `PlanSummary` (`src/components/plan-summary.tsx`)
- プラン概要カードコンポーネント
- 統計情報、栄養バランス、タグ分析を表示
- Pending状態の時のみ表示

### `DayCard`
- 日別カードコンポーネント
- `isPending`プロップで表示モードを切り替え:
  - `true`: 詳細表示（タイトル全文、タグ、PFC）
  - `false`: 簡易表示（タイトル切り詰め、カロリーのみ）

---

## API エンドポイント

### `POST /api/plan/generate`
プラン生成を開始

**リクエスト:**
```json
{
  "userId": "user-id"
}
```

**レスポンス:**
```json
{
  "status": "started",
  "message": "プラン生成を開始しました"
}
```

### `POST /api/plan/approve`
プランを承認

**リクエスト:**
```json
{
  "userId": "user-id",
  "planId": "plan-id"
}
```

**レスポンス:**
```json
{
  "success": true,
  "message": "プランを承認しました。レシピ詳細を生成中です。"
}
```

### `POST /api/plan/reject`
プランを拒否

**リクエスト:**
```json
{
  "userId": "user-id",
  "planId": "plan-id"
}
```

**レスポンス:**
```json
{
  "success": true,
  "message": "プランを拒否しました。"
}
```

---

## 注意事項

1. **レシピ詳細生成のタイミング**
   - 承認後にバックグラウンドで生成されるため、承認直後はレシピ詳細が未生成の可能性がある
   - ユーザーには「完了まで1〜2分かかる場合があります」と通知

2. **バッチ処理の制限**
   - 5食ずつ、並列3件、バッチ間に1秒待機
   - APIレート制限を考慮した設計

3. **買い物リスト生成**
   - レシピ詳細がすべて生成されてから買い物リストを生成
   - 材料を集計してカテゴリ別に分類

4. **既存プランとの互換性**
   - `active`状態のプランは既存の表示を維持
   - `pending`状態のプランのみ承認UIを表示

---

## テスト実行結果

### シナリオ2-1: プランなし状態からプラン作成

**実行日**: 2026-01-24  
**実行モード**: ゲストモード

**テストステップ:**

1. **プラン画面へのアクセス**
   - [x] ナビゲーションから「プラン」をタップ
   - [x] プラン画面（`/plan`）が表示されることを確認

2. **プランなし画面の確認**
   - [x] 「プランがありません」メッセージが表示されることを確認
   - [x] 「まずは2週間のプランを作成しましょう」説明が表示されることを確認
   - [x] 「プランを作成する」ボタンが表示されることを確認

3. **プラン作成開始**
   - [x] 「プランを作成する」ボタンをクリック
   - [x] プラン作成中画面が表示されることを確認

4. **プラン生成完了の確認**
   - [x] 1〜2分待機
   - [x] Pending状態のプラン表示に切り替わることを確認

**結果**: ✅ 成功。プラン作成からPending状態のプラン表示まで正常に完了。

---

### シナリオ2-2: プラン作成中の状態確認

**実行日**: 2026-01-24  
**実行モード**: ゲストモード

**テストステップ:**

1. **プラン作成中画面の表示確認**
   - [x] プラン画面にアクセス
   - [x] プラン作成中画面が表示されることを確認（「プラン作成中...」「AIが14日間の食事プランを生成しています。作成には1〜2分かかります。」）

2. **定期的な状態チェックの確認**
   - [ ] 5秒ごとにプロフィールとプランが再取得されることを確認（開発者ツールで確認）

3. **プラン生成完了後の自動遷移確認**
   - [x] プラン生成完了後、自動的にPending状態のプラン表示に切り替わることを確認

**結果**: ✅ 成功。プラン作成中画面が正しく表示され、自動的にPending状態に遷移したことを確認。

---

### シナリオ3-1: プラン承認フロー

**実行日**: 2026-01-24  
**実行モード**: ゲストモード

**テストステップ:**

1. **Pending状態のプラン表示確認**
   - [x] 「承認待ち」バッジが表示されることを確認
   - [x] プラン概要カードが表示されることを確認（14日間の平均栄養価、使用ジャンル・タグ、チートデイ情報）
   - [x] 日別カードが詳細表示されることを確認（レシピタイトル全文、タグ、栄養価）

2. **承認ボタンの確認**
   - [x] 「このプランで進める」ボタンが表示されることを確認
   - [x] 「別のプランを生成する」ボタンが表示されることを確認

3. **プラン承認実行**
   - [x] 「このプランで進める」ボタンをクリック
   - [x] 承認処理が開始されることを確認

4. **承認完了の確認**
   - [x] Active状態のプラン表示に切り替わることを確認
   - [x] 「承認待ち」バッジが消えることを確認
   - [x] 「飽きた」ボタンと「一括再生成」ボタンが表示されることを確認
   - [x] 日別カードが簡易表示に切り替わることを確認（レシピタイトルがリンク）

**結果**: ✅ 成功。プラン承認からActive状態への遷移が正常に完了。

---

### シナリオ3-2: プラン拒否フロー

**実行日**: 2026-01-24  
**実行モード**: ゲストモード

**テストステップ:**

1. **拒否ボタンの確認**
   - [x] 「別のプランを生成する」ボタンが表示されることを確認

2. **プラン拒否実行**
   - [x] 「別のプランを生成する」ボタンをクリック
   - [x] 確認ダイアログが表示されることを確認

3. **確認ダイアログでの操作**
   - [x] 「キャンセル」をクリック → ダイアログが閉じ、プランがそのまま表示されることを確認
   - [x] 再度「別のプランを生成する」ボタンをクリック
   - [x] 「OK」をクリック

4. **拒否完了の確認**
   - [x] プランがアーカイブされることを確認（プランなし状態または新しいプランが生成される）

**結果**: ✅ 成功。プラン拒否フローが正常に動作し、確認ダイアログの操作も正常に動作することを確認。

---

### シナリオ4-8: プラン画面での操作（Active状態）

**実行日**: 2026-01-24  
**実行モード**: ゲストモード

**テストステップ:**

1. **Active状態のプラン表示確認**
   - [x] 「2週間プラン」タイトルが表示されることを確認
   - [x] 開始日が表示されることを確認
   - [x] 「飽きた」ボタンと「一括再生成」ボタンが表示されることを確認
   - [x] 日別カードが簡易表示されることを確認（レシピタイトルがリンク、カロリーのみ表示）

2. **レシピ詳細への遷移**
   - [x] 日別カードのレシピタイトルをクリック
   - [x] レシピ詳細ページに遷移することを確認（レシピタイトル、栄養情報、タグ、材料、手順が表示される）

3. **「飽きた」ボタンの操作**
   - [ ] 「飽きた」ボタンをクリック
   - [ ] 飽き防止ダイアログが表示されることを確認

4. **「一括再生成」ボタンの操作**
   - [x] 「一括再生成」ボタンをクリック
   - [x] 確認ダイアログが表示されることを確認
   - [x] 「キャンセル」をクリック → ダイアログが閉じ、プランがそのまま表示されることを確認
   - [x] 再度「一括再生成」ボタンをクリック
   - [x] 「OK」をクリック
   - [x] プラン生成が開始されることを確認（プラン作成中画面または新しいPendingプランが表示される）

**結果**: ✅ 成功。Active状態のプラン表示、レシピ詳細への遷移、確認ダイアログの操作、一括再生成の実行を確認。
